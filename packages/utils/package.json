{
  "name": "@saas-events/utils",
  "version": "2.0.0",
  "private": true,
  "description": "Fonctions utilitaires pures partagées (validation, parsing durée, sanitisation)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
