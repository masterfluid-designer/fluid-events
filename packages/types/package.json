{
  "name": "@saas-events/types",
  "version": "2.0.0",
  "private": true,
  "description": "Types TypeScript partagés entre frontend et backend",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "rm -f tsconfig.build.tsbuildinfo && tsc -p tsconfig.build.json --watch --preserveWatchOutput --watchFile PriorityPollingInterval --watchDirectory DynamicPriorityPolling",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
